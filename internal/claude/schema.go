package claude

import "github.com/agentop-dev/agentop/internal/adapter"

// RawEvent is a type alias for adapter.Event.
type RawEvent = adapter.Event

// RawMessage is a type alias for adapter.Message.
type RawMessage = adapter.Message

// Usage is a type alias for adapter.Usage.
type Usage = adapter.Usage

// CacheTiers is a type alias for adapter.CacheTiers.
type CacheTiers = adapter.CacheTiers

// Content is a type alias for adapter.Content.
type Content = adapter.Content

// FileSnapshot is a type alias for adapter.FileSnapshot.
type FileSnapshot = adapter.FileSnapshot

// SessionMeta is a type alias for adapter.SessionMeta.
type SessionMeta = adapter.SessionMeta
